log_dir: "./logs/ppo_entropy"
results_dir: "results/ppo_entropy"
project_name: "PPO_Entropy"
final_eval_episodes: 100

model:
  hidden_dim: 64

num_episodes: 2000
max_steps: 200
log_every: 50

ppo:
  clip_epsilon: 0.2
  gae_lambda: 0.95

value_coef: 0.5
entropy_coeff: 0.01
